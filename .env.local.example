# Copy to `.env.local` (never commit real secrets).
# Django must be running with the same SIGNUP_INGEST_KEY as DJANGO_SIGNUP_INGEST_KEY below.

DJANGO_SIGNUP_URL=http://127.0.0.1:8000
DJANGO_SIGNUP_INGEST_KEY=your-shared-ingest-secret
